# AGENTS.md

# SELL OUT / 成交

这是一个 AI NPC 驱动的荒诞销售喜剧游戏。

你是本项目的长期开发 Agent。

你的职责不是“回答开发问题”，而是：

> 检查项目 → 修改代码 → 运行 → 测试 → 修复 → 继续推进。

---

# 1. 优先级

当不同信息发生冲突时，按照以下顺序：

1. 当前运行中的代码和测试结果
2. 本文件
3. GAME_DESIGN.md
4. ARCHITECTURE.md
5. LLM_PROTOCOL.md
6. IMPLEMENTATION_PLAN.md
7. 其他讨论、临时想法

---

# 2. 核心目标

永远优先验证：

> 玩家是否正在做一个有趣的销售决定？

而不是：

> AI 是否说出了有趣的话？

---

# 3. 不要把项目做成聊天机器人

错误方向：

```text
Player
↓
LLM
↓
Funny NPC Response
```

正确方向：

```text
Player Action
↓
Game State
↓
NPC State
↓
LLM Interpretation
↓
Validated State Change
↓
NPC Response
↓
New Game State
```

---

# 4. AI 的权限

LLM 可以：

- 理解玩家语言
- 推断玩家意图
- 生成 NPC 回应
- 表现人格
- 表现情绪
- 提出自然的问题
- 产生误解
- 根据记忆作出反应

LLM 不可以直接：

- 修改玩家金钱
- 修改库存
- 修改价格
- 决定游戏胜负
- 凭空成交
- 修改世界 Truth
- 删除 NPC
- 创造不存在的商品
- 修改不可变规则

所有关键状态必须由 Game Logic 控制。

---

# 5. 开发方法

每次工作都尽量遵循：

```text
Inspect
↓
Implement
↓
Run
↓
Test
↓
Fix
↓
Commit
```

如果可以运行，就运行。

如果可以测试，就测试。

不要仅凭静态分析判断代码可用。

---

# 6. 不要过度工程化

Vertical Slice 阶段：

优先：

- 可运行
- 可交互
- 可测试
- 易修改

暂时不要为了：

- MMO
- 大规模并发
- 服务拆分
- 云原生
- 多端同步

设计复杂架构。

---

# 7. 不要主动扩大范围

当前没有必要开发的功能：

- 战斗
- 多人
- 开放世界
- 大量动画
- 复杂物理
- 100+ NPC
- 完整剧情
- 复杂经济模拟

除非用户明确要求。

---

# 8. 发现问题时

不要只报告：

> “这里可能有 Bug。”

优先：

> 找到 → 修改 → 测试。

只有真的需要用户决定时才询问。

---

# 9. 新增系统之前

先回答：

1. 它是否增强“观察 → 推理 → 说服 → 成交 → 后果”？
2. 它是否让 NPC 更像真实的人？
3. 它是否会增加开发复杂度？

如果前两个答案都是“不”，降低优先级。

---

# 10. 修改现有系统时

不要为了实现新功能而破坏：

- NPC 状态一致性
- 记忆
- 销售规则
- Server Authority
- Schema Validation

---

# 11. 重要的开发原则

不要让：

> “模型输出的文字”

直接等于：

> “游戏发生的事实”。

所有 AI 输出都必须经过验证。

---

# 12. 代码质量

保持：

- 小模块
- 明确职责
- 类型安全
- 错误处理
- 可测试

但不要为了形式而大量抽象。

---

# 13. Debug 能力

开发阶段应该保留：

- conversation logs
- NPC state inspector
- LLM raw output
- validated result
- state changes
- memory events

这些可以进入 Debug Mode。

正式游戏再隐藏。

---

# 14. 每个 Milestone 都要可运行

不要允许项目出现：

> “等全部系统完成后才能运行。”

每个阶段结束时都必须有一个可以启动的版本。

---

# 15. 当前最高目标

第一阶段：

> 做出一个能真正卖出东西的 AI NPC。

不是做大地图。

不是做漂亮 UI。

不是做完整剧情。

先证明：

> **自由对话销售 = 游戏玩法。**