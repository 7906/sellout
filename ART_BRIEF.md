# SELL OUT / 成交 — 美术优化需求书

> 本文档是美术优化的完整需求与交付定义。执行前请完整阅读，特别是「硬性约束」和「验收标准」。
> 需求方已定案：不要重新设计游戏玩法，只做视觉升级。

---

## 1. 项目背景

《SELL OUT / 成交》是一款 AI NPC 驱动的荒诞销售喜剧游戏（题材参考《Suck Up!》）：玩家扮演推销员，敲开 7 位居民的门，自由对话套近乎，把商品推销出去赚钱。

- 技术栈：Phaser 4.2.1 + Vite 6 + TypeScript（client），Fastify（server）
- 游戏画布：**960 × 600**，固定尺寸
- 玩法已完整可玩（对话、成交、赚钱），本次**只改视觉，不动逻辑**

### 如何运行（验收时必须实际跑起来）

```bash
# 仓库根目录，两个终端
npm run dev:server   # 游戏服务器 → http://127.0.0.1:3001
npm run dev:client   # 前端 → http://127.0.0.1:5173
```

### 当前画面现状（全部是彩色矩形占位）

场景文件 `client/src/scenes/TownScene.ts`：
- 天空带（0x6f8f9f，y<200）、马路（y≈255-365，白色虚线）、人行道（y≈385-455）
- 5 家店铺：120×130 矩形 @ y≈95-225，横排（x=90/250/410/570/730），标签：咖啡店/便利店/裁缝铺/公园入口/公交站
- 玩家：28×44 蓝色矩形（WASD/方向键移动，y 限制在 380-580）
- 7 个 NPC：30×46 彩色矩形，站在人行道 y≈430，从 `/api/game` 动态拉取生成（数量与 id 不固定写死），头顶 emoji+名字标签，有呼吸浮动 tween 和悬停气泡
- HTML/CSS 覆盖层：右侧聊天面板（深灰 #2b2f33）、HUD（金钱+货品包+LOG 按钮）

---

## 2. 美术方向（已定案）

**风格**：扁平卡通插画（flat cartoon illustration），**粗描边**（统一 3-5px 深色描边，色值约 #2E2620），**高饱和暖色调**，造型适度夸张（大头身比约 1:3~1:4，喜剧感）。

**题材氛围**：中国老小区街道——砖红/米黄墙面、卷帘门店铺、晾衣杆、自行车、小广告、电线。要有生活气息和一点荒诞喜感（比如某家门上贴着「谢绝推销」）。

**参考游戏**：《Suck Up!》（题材与夸张感）、《中国式家长》（中国市井卡通人物）、《Papers, Please》仅参考构图密度（不取其灰暗）。

**调色板基准**（UI 需与此协调）：

| 用途 | 色值 |
| --- | --- |
| 天空 | #7EC8C8 |
| 马路 | #4A4A55 |
| 人行道 | #C9BFA8 |
| 建筑砖红 | #B5573B |
| 建筑米黄 | #E8D8B0 |
| 点缀红（招牌/灯笼/春联） | #D9534F |
| 点缀绿（树/公园） | #6B9E4E |
| 描边 | #2E2620 |
| UI 面板（保留现深灰系） | #2B2F33 / #23272B |

> 备选风格（仅当执行方图像生成无法稳定保持上述风格时）：统一规格像素风（64×96 角色， @3x 显示）。两种风格不得混用。

---

## 3. 资产清单（精确交付）

所有 PNG 放入 **`client/public/assets/`**，透明背景，@2x 绘制（引擎缩小显示）。

| 文件名 | 尺寸(px) | 显示尺寸 | 内容 |
| --- | --- | --- | --- |
| `bg_street.png` | 1920×1200 | 960×600 | 完整街道背景（见 §2 构图要求） |
| `npc_frugal_uncle.png` | 256×384 | 约64×96 | 钱大叔 |
| `npc_teacher_li.png` | 256×384 | 同上 | 李老师 |
| `npc_sister_zhang.png` | 256×384 | 同上 | 张姐 |
| `npc_grandma_chen.png` | 256×384 | 同上 | 陈奶奶 |
| `npc_lao_zhou.png` | 256×384 | 同上 | 老周 |
| `npc_da_liu.png` | 256×384 | 同上 | 大刘 |
| `npc_xiao_ai.png` | 256×384 | 同上 | 小艾 |
| `player.png` | 256×384 | 约56×84 | 玩家推销员 |

**构图要求（bg_street）**：保留现有三段式纵向结构（上 1/3 建筑+店铺、中 1/5 马路、下为两侧人行道），5 家店铺招牌文字保留（咖啡店/便利店/裁缝铺/公园入口/公交站），**人行道区域必须保持开阔平整**（NPC 要站在上面），画面四角留出可读空间（左上角有游戏标题文字，右侧有聊天面板悬浮）。背景里可埋荒诞细节小彩蛋（墙上小广告「通马桶 138-xxxx」、贴着「谢绝推销」的门）。

**7 位 NPC 视觉设定**（与角色人设一致，必须可区分）：

| NPC | 外形要点 |
| --- | --- |
| 钱大叔（退休工人·教学关） | 63岁，灰白短发，旧夹克，胸挂老花镜，手端茶缸，表情精明带笑 |
| 李老师（退休教师·最难） | 68岁，烫发+细框眼镜，深色开衫，抱教案和红笔，严厉皱眉 |
| 张姐（高三家长） | 42岁，围裙+居家服，微憔悴，欲言又止的温和表情，背景可暗示屋内 |
| 陈奶奶（独居老人·道德关） | 78岁，白发髻+蓝布衫，拐杖，脚边可有小泰迪「豆豆」，慈祥 |
| 老周（下岗工人） | 45岁，旧西装+松领带，皮鞋擦得亮但袖口磨毛，讨好又焦虑的笑 |
| 大刘（程序员·被坑过） | 40岁，格子衫，黑眼圈，抱臂黑脸，胸前挂工牌 |
| 小艾（同行卧底） | 26岁，连帽卫衣+墨镜（墨镜可推到额头），举着手机假笑，明显在录音 |
| 玩家（推销员） | 年轻人，白衬衫+工牌，背样品双肩包，一手提样品箱，憨厚热情 |

---

## 4. 代码改动范围（只允许改这些）

| 文件 | 改动 |
| --- | --- |
| `client/src/scenes/TownScene.ts` | 背景图替换矩形绘制；NPC/玩家换 sprite（`this.add.image` / `setDisplaySize`），保留名字标签、呼吸 tween、悬停气泡（气泡可升级为描边高亮/亮度变化）；点击开聊天逻辑不动 |
| 新增 `client/src/scenes/PreloadScene.ts` | Phaser loader 预加载 §3 全部资产；**任一资产加载失败 → 该对象退回现有矩形占位，游戏不得崩溃** |
| `client/src/main.ts` | scene 数组加入 PreloadScene |
| `client/src/styles/overlay.css` | 聊天面板/HUD 配色微调至与 §2 调色板协调（CSS 变量优先），布局结构不动 |
| 新增 `client/public/assets/*` | §3 资产文件 |

**7 个 NPC 由 `/api/game` 动态返回**（id：frugal_uncle / teacher_li / sister_zhang / grandma_chen / lao_zhou / da_liu / xiao_ai），sprite 必须按 `npc_<id>.png` 动态取用，未知 id 退回矩形。

---

## 5. 硬性约束（违反任何一条即返工）

1. **禁止改动** `server/`、`shared/` 目录及任何 API 契约（只能读 `/api/game` 等既有接口）
2. **禁止改动游戏逻辑**：移动、成交、数值、聊天流程、递合同、HUD 功能全部保持原行为
3. 不引入新依赖（不加 npm 包；加载用 Phaser 自带 loader）
4. 不改画布尺寸（960×600）与响应式行为
5. 每完成一批改动必须：`npm run typecheck` 0 错误 + `npm run build -w client` 通过 + 实际启动两个服务在浏览器中走通「点击 NPC → 聊天 → 递合同」
6. 全部 8 个角色 + 背景**风格严格一致**（同一套描边/配色/头身比），宁可口味统一，不要单张惊艳
7. 中文文字（店铺招牌、标题、NPC 名标签）必须在最终画面中清晰可读

---

## 6. 验收标准（逐条核对）

- [ ] `npm run typecheck` 0 错误；`npm run build -w client` 通过
- [ ] 浏览器实测：背景与 5 家店铺正常显示，招牌文字可读
- [ ] 7 个 NPC 全部以 sprite 呈现，形象与 §3 设定一一对应，名字标签可读
- [ ] 点击任意 NPC 打开聊天（开局即敲门状态），对话、递合同功能正常
- [ ] 玩家 sprite 可用 WASD/方向键在人行道区域移动，边界限制不变
- [ ] 删除任一资产文件后游戏不崩溃（矩形占位回退）
- [ ] 聊天面板、HUD、复盘页（/log-viewer.html）与新美术色调协调
- [ ] 交付截图：①主场景全景 ②与某 NPC 聊天中 ③HUD 货品包展开，共 3 张
- [ ] 交付说明：新建/修改文件清单 + 资产文件对照表

---

## 7. 非目标（不要做）

- ❌ 音效/音乐
- ❌ 角色行走/说话动画（保留现有呼吸浮动即可；悬停反馈用亮度/缩放）
- ❌ 昼夜循环、天气、粒子特效
- ❌ 场景切换/多地图/过场
- ❌ 服务端与玩法的任何改动
- ❌ 角色立绘/头像大图（聊天面板暂用 emoji+文字，后续再说）

---

## 8. 建议工作流程

1. 先跑起来现状游戏（§1），截图记录占位画面基线
2. 生成/绘制 `bg_street.png` → 集成 → 截图比对构图约束
3. 生成 8 个角色 sprite（一次定风格，再批量出其余 7 个，保证一致性）
4. 集成 + PreloadScene 回退逻辑 → 浏览器全功能回归
5. CSS 配色协调 → 三张验收截图 → 按 §6 清单自查后交付
