# SALES_DESIGN.md — 销售规则设计：角色成交条件 × 商品目录

> 本文档是两块内容的设计方案（待评审，未实现）：
> **A 部分**：把已有的死数据（dealConditions / flags / interest）变成真正的成交规则；
> **B 部分**：商品目录按高中低价位重组，给出备选池与入选理由。
>
> 原则对齐 AGENTS.md：所有判定由 Server 权威结算；LLM 只做「语义认定」并打标，
> 打标结果经 Zod 校验白名单后才允许改变状态（§11：模型输出的文字 ≠ 游戏事实）。

---

# A 部分：角色成交条件系统

## A1. 现状盘点（为什么现在「雷区」是软的）

数据已备好但**没有任何服务器逻辑消费**（2026-08-30 核查）：

| 字段 | 数据现状 | 逻辑现状 |
| --- | --- | --- |
| `dealConditions` | 7 个 NPC 全部填了条件 | `evaluatePurchase` 不读它，成交只看 信任/怀疑/预算/重复购买 |
| `flags.noInstall` | 李老师 = true | 无关键词检测，只靠 LLM 看人设即兴发挥 |
| `flags.kindTarget` | 陈奶奶 = true | 无任何结算差异 |
| `interest` | LLM 会输出 interest 效果、UI 在显示 | 闸门不看，是「假数值」 |
| `memoryCandidates` | 协议 §12 已定义 | promptBuilder 明确要求输出空数组，通道闲置 |

后果：玩家踩张姐丈夫的雷、对李老师吹牛，只会掉点心情数值，照样可能成交——
「观察 → 推理 → 说服 → 成交」里的**推理环节不影响结果**。

## A2. 设计原则

1. **两条事实通道，server authority 不变**：
   - **行为类条件**：服务器可直接观测（递合同太早、抢报价、成交间隔），纯计数，零 LLM 参与。
   - **语义类条件**（「让 TA 笑」「真诚道歉」）：LLM 认定打标 → **白名单校验** → 服务器记账。
     LLM 只能说「这个行为发生了」，永远不能直接改闸门、改数值。
2. **flag 是 NPC 身上的事实记录**（类似 `purchased`），写进存档，可跨回合查询；不是一次性判定。
3. **达成必须可感知**：条件达成时 NPC 台词要有反应（LLM 在 prompt 里知道「哪些条件已达成」，
   会自然念叨「今天这销售，倒是有几句人话」）。玩家看不见反馈 = 没法推理 = 系统白做。
4. **desc 不直接下发客户端**（保持 Server-only），玩家靠对话试探发现——这是玩法本身。

## A3. 条件类型系统（schema 不变，语义补全）

沿用现有 `DealCondition`，三种语义：

```ts
interface DealCondition {
  id: string;
  flag: string;        // 写入 npc.conditionFlags 的键名
  negative?: boolean;  // 反向条件：触发即「今天判死」
  count?: number;      // 计数型：flag 累计 ≥ count 才算达成
  desc: string;        // 调试用，不下发客户端
}
```

- **正向**：`laughed`、`pet_mentioned`、`apology_sincere`、`family_mentioned`、`objections_handled(count:2)`
- **反向**：`oversell_detected`（吹牛被识破）、`price_first_by_player`（抢报价）
- **计数**：`objections_handled` 需 ≥2

## A4. 检测通道（三个，各管各的）

### 通道 1：服务器行为判定（零 LLM，可自动化测试）

| 条件 | 判定逻辑 |
| --- | --- |
| `price_first_by_player`（反向） | 玩家消息命中报价模式 `/\d{2,5}\s*(元|块|¥)/` 且本会话 NPC 从未问过价 → 记 flag |
| 「不想听你说话」软消气 | 明确拒绝/送客后 3 回合内递合同显示 npc_refusing，超过自动过期恢复真实拒绝原因（防死档感） |
| 递合同太早 / 成交冷却 | 已有（MIN_EXCHANGES / SALE_COOLDOWN），纳入同一 reason 体系 |

### 通道 2：LLM 语义认定（新增结构化输出字段）

LLMResponse 增加一个字段：

```jsonc
"conditionHints": ["make_laugh", "oversell_detected"]   // 只能从本 NPC 的条件 id 列表里选
```

- prompt 注入本 NPC 的**条件 id + 判定标准**（如 `make_laugh: 玩家的玩笑让 TA 真心笑了`），
  要求 NPC 台词对达成做出人格化反应。
- 服务器侧 Zod 校验：不在白名单内的 id 直接丢弃（防幻觉）。
- **防误伤（双重确认）**：`make_laugh` 需同时满足 `emotion ∈ {amused, excited}`；
  `oversell_detected` 需**同时满足** `intent ∈ {show_suspicion, refuse}` 且怀疑值本回合真的上升
  （2026-08-30 实测收紧：最难缠的 NPC 听谁推销都摆怀疑脸，单看 intent 会把正常推销误判成吹牛，
  用户实测护膝 4 连拒后修正为 AND 确认）。
  条件不满足则 hint 无效——LLM 说玩家吹牛但 NPC 没起疑，就不算识破。
  `price_first_by_player` 完全不收 hint，只认服务器正则（实测模型会对没报价的话误标）。
- `objections_handled`：NPC intent=refuse/counter_offer 的**下一回合**，若信任未下降 → 计数 +1。

### 通道 3：关键词规则（flags 的服务器化）

| flag | 规则 |
| --- | --- |
| `noInstall`（李老师） | 玩家消息含「分期|0首付|月供|贷款」→ 立即结算 `trust_down_medium + suspicion_up_medium + patience_down_small`，强制 mood=angry，本回合禁止自动成交（LLM 就算 agree 也无效）；信任大挫后当天内基本过不了闸。关键词判定服务器做（可靠可测），LLM 只负责把「被激怒」演出来 |
| `kindTarget`（陈奶奶） | 结算差异：卖给她单价 > ¥300 的商品时，收工日报出现「良心账」条目（喜剧化道德提示，纯文案）；不给数值惩罚——道德关的压力应来自玩家自己 |

## A5. 闸门集成（evaluatePurchase 扩展后的裁决顺序）

```text
1. already_owned          「TA 已经买过了」
2. negative flag 当日触发  「你踩了 TA 的雷」（condition_failed）→ 当次必拒 + 额外惩罚效果
3. 正向条件未达成          「TA 还没被打动到那个点」（condition_not_met）
4. trust < 门槛            「TA 还不够信你」
5. suspicion ≥ 50          「TA 的怀疑太高了」
6. interest < 商品门槛      「TA 对这个没兴趣」（见 A7）
7. budget < 价格           「TA 买不起这个」
```

客户端 REJECT_REASONS 增加两条中文文案；被拒代价维持现有 REJECT_EFFECTS。

## A6. conditionFlags 的存储与生命周期

```ts
// NPCState 新增（server-only，进存档 v3）
conditionFlags: Record<string, { count: number; setAtDay: number }>;
```

- **正向 flag 跨天保留**：真诚道歉过的人不会一夜忘掉（对齐 PHASE 12 记忆原则）。
- **反向 flag 只管当天**：被吹牛坑一次，李老师今天不谈了，隔夜愿意再给机会——
  但 suspicion 代价照记不退。惩罚即时、明确，且不产生永久死档。

## A7. interest 参与裁决（从假数值变真系统）

规则：**成交还要求 `interest ≥ 商品兴趣门槛`**，门槛只跟价格挂钩：

```ts
function interestGateFor(product: Product): number {
  if (product.price >= 1000) return 40;  // 大件：必须聊出真实需求
  if (product.price >= 250)  return 25;  // 中件：聊到相关话题
  return 0;                              // 小件：不看兴趣，靠信任
}
```

- 设计意图：便宜货人人可以顺手买（走量），贵的货必须「TA 自己动了念头」——
  玩家想清高价库存（佣金高），就必须先挖需求，这正是 GAME_DESIGN §7 的 Need System 循环。
- REJECT reason：`interest_low` →「TA 对这个还没动心思」。

## A8. 七人条件落地表（数据已就位，此处为接线后的完整语义）

| NPC | 正向条件 | 反向条件 | 检测通道 | 达成反馈设计 |
| --- | --- | --- | --- | --- |
| 钱大叔（教学关） | `make_laugh` 让他真心笑一次 | `price_first_by_player` 别抢报价 | LLM+情绪双确认 / 正则 | 笑了之后主动问「你说啥来着？」；被抢报价当场冷脸 |
| 李老师（BOSS） | `objections_handled≥2` 接住两个异议 | `oversell_detected` 吹牛即死；`noInstall` 提分期即死 | LLM+怀疑双确认 / 关键词 | 接住异议后开始翻检测报告；吹牛当场背课文式驳斥 |
| 张姐 | `family_mentioned` 真聊到女儿 | （提丈夫：雷区走 prompt + LLM 自由发挥，trust 重挫即可，不做硬条件——避免「一句话死档」太苛刻） | LLM 认定 | 聊到女儿开始掏心窝 |
| 陈奶奶 | `pet_mentioned` 聊到豆豆 + `make_laugh` | —— | LLM 认定（+amused 双确认） | 聊到豆豆话匣子打开 |
| 老周 | `family_mentioned` 真诚聊到儿子 | `oversell_detected` 画大饼即翻脸 | LLM 认定 / LLM+情绪双确认 | 聊到儿子眼里有光；被画饼当场掀桌 |
| 大刘 | `apology_sincere` + `objections_handled≥2` | —— | LLM 认定 / refuse 后信任不降 | 道歉后「……行吧，你倒是实诚」 |
| 小艾 | `family_mentioned`（她好奇这招） | `oversell_detected` 记进避雷笔记 | LLM 认定 / LLM+怀疑双确认 | 记笔记的声音变欢快 |

> 设计取舍：故意**不给每个 NPC 堆满条件**。教学关 1 正 1 反，BOSS 1 正 2 反，梯度即难度曲线。
> 「提张姐丈夫」这类已由 prompt + 情绪微结算覆盖的雷区不做成硬条件，防止误伤式死档过多。

## A9. 上下文同步：自我披露账本（评审新增，修「NPC 不记得自己说过什么」）

### A9.0 现象与根因（实测复现）

玩家「张姐你好」→ 张姐「你怎么知道我名字」——**这是对的**（陌生拜访设定：玩家看得见头顶名牌，NPC 不认识玩家，信息不对称是设计，保留）。

但接下来：张姐自己说闺女升学 → 玩家接话问闺女 → 张姐「你怎么知道的」——**错**。三个叠加根因：

1. **无披露账本**：prompt 只说「心里惦记的事是隐私，被戳到要回避」。每回合 LLM 都要从原始对话里自行推断「这话是谁先说的」，flash 级模型经常推错；
2. **历史截断**：`buildHistoryMessages` 只带最近 12 条（`slice(-12)`），6 个回合后早期披露滚出窗口，LLM 物理上看不到了；
3. **规则偏置**：effects 规则里「说了不该知道的事 → suspicion_up」没有对称保护——「该知道的事」（她自己说过的）没有任何机制兜底。

### A9.1 方案：账本进「状态」，不进「历史」（历史会截断，状态不会）

1. **knowledge / worries 数据结构化**（npcData 一次性改造）：

```ts
// 现在是裸字符串数组，改为：
knowledge: [{ id: "k1", text: "我女儿高三……", tags: ["女儿", "高三", "闺女"] }]
worries:   [{ id: "w1", text: "丈夫失业……",  tags: ["丈夫", "老公", "失业", "那口子"] }]
```

   `tags` 是 2-4 字中文关键词，供**服务器**匹配玩家消息（不进 prompt）。7 个 NPC 全量补全，实现时列清单并跑 detect 式冲突检查（「闺女」不得误伤「姑娘」类寒暄——匹配用「包含即命中 + 全角/口语词并列」策略）。

2. **LLM 每回合申报披露**：LLMResponse 新增 `revealed: string[]`——「本次回复里我亲口说出口的私事」的 id 列表。Zod 白名单校验：只认注入过的 id，非法 id 丢弃（防幻觉）。

3. **服务器记账**：ConversationService 会话内 `disclosedIds: Set<string>`，与 histories 同生命周期（跨天清空——聊天线程本来就是每天重开；购买记忆走 purchased，不混用）。

4. **每回合注入账本区块**（替代现在那句无保护的「陌生人不知道」）：

```text
# 你已经亲口告诉过他的事（他知道这些，是你说的，不是他神通广大）
· [w1] 女儿高三，压力大到腰疼
他接这些话头时是正常聊天：不要惊讶、不要问「你怎么知道」、不要加怀疑效果。
只有当 TA 说出【你从没说过、上面列表里也没有】的事，你才需要警觉「TA 怎么知道」。
```

   账本是**状态**：第 20 回合依然完整注入，彻底绕开 slice(-12) 截断。

5. **服务器效果闸门（防御纵深，AGENTS.md §11）**：结算 effects 前做 tag 匹配——
   - 玩家消息命中【已披露】项的 tag → **本回合丢弃全部 suspicion_up 效果**（信息来自她自己，起疑无理）；
   - 命中【未披露】项的 tag → suspicion_up 照常生效（这正是「说了不该知道的事」，雷区依然疼）。
   - 失败方向安全：误丢 suspicion_up 只是 NPC 少疑一分，不可能帮玩家凑信任或强行成交。

### A9.2 边界与取舍

- hello 开场白不记账（场面话），只有 `revealed` 申报才算披露；
- 「你怎么知道我名字」的**首次**反问保留——设定趣味；账本防的是「反复追问自己说过 / 玩家已解释过的事」；
- tags 一鱼两吃：A8 的语义条件（`family_mentioned` = 玩家消息命中女儿/儿子 tag）用同一套 tag 做服务器侧旁证；
- 一切不改变：LLM 仍然决定台词与情绪，服务器只管「哪些事实已披露」这个可验证的事实账。

## A10. 验收断言（实现后追加进 dayloop-check）

1. 正向条件未达成时递合同 → `condition_not_met`，不结算。
2. LLM hint 白名单外的 id 被丢弃，不产生 flag。
3. `make_laugh` 无 amused 情绪配合 → 双重确认失败，不记账。
3b. 模型对「叔」这种没报价的话误标 no_price_first + 怀疑上升 → 不记账；真报价「299块」→ 正则记账。
3c. oversell 收紧后：intent=show_suspicion 但怀疑值没涨 → 不记账（A5b 断言）。
4. 李老师消息含「分期」→ 立即 angry + 三重惩罚效果；激怒回合 LLM agree 也不结算，随后递合同因信任崩塌被拒。
5. `oversell_detected` 当天再递 → `condition_failed`；跨天 flag 清空后可再谈，suspicion 不回退。
6. `objections_handled`：refuse 后一回合信任没降 → count=1；再成一次 → 达标放行。
7. ¥299 拖把 interest 20 → `interest_low` 被拒；聊到 25+ 放行。¥89 棉拖不看兴趣。
8. conditionFlags 进存档、重启恢复；正向跨天在、反向跨天无。
9. 张姐回合：假 LLM 回复带 `revealed:["w1"]` → `disclosedIds` 含 w1；下一回合注入的 system prompt 含「已经亲口告诉过他」区块与该条目文本。
10. 玩家消息命中已披露 tag（「女儿」）→ 本回合 suspicion_up_small 被服务器丢弃；命中未披露 tag（「丈夫」）→ suspicion_up 保留生效。
11. 聊到 15+ 回合（历史窗口已截断早期消息）→ 账本区块依然完整注入（状态不随 slice(-12) 丢失）。
12. `revealed` 含非法 id → 静默丢弃，不记账、不报错；`revealed` 缺字段 → 视为空数组。

---

# B 部分：商品目录（高中低三档重组方案）

## B1. 现状问题

1. **6 件常规货挤在 ¥89–399**：李老师（¥20000）和钱大叔（¥9000）有钱没处花——
   高预算 NPC 没有大单可成交，赚钱效率平（一单最多 ¥100）。
2. **预算分布断层**：陈奶奶 ¥1200 / 老周 ¥300 几乎只买得起棉拖和保温杯；
   6 件荒诞特殊货里 4 件（¥2999–¥99999）全目录只有 1–2 个 NPC 买得起，
   随机刷到「便携式地下室」时对多数玩家等于死任务。
3. **佣金曲线平**：¥30–100，特殊货里防猫背叛保险（★3 难度）佣金 ¥60 + 奖金 ¥50，
   收益还不如一把拖把——难度与回报倒挂。
4. **功能重叠**：保温杯/保鲜盒/拖把都吃「居家实需」一个篮子，缺「情感型」「对症型」商品。

## B2. 价位档定义（依据 NPC 预算分布）

| 档位 | 价格带 | 佣金率* | 主攻 NPC | 定位 |
| --- | --- | --- | --- | --- |
| 低 | ¥50–150 | 30–35% | 陈奶奶(1200)、老周(300) | 入场券：最穷的人也买得起点什么，走量练手 |
| 中 | ¥150–400 | 25–32% | 钱大叔、张姐、大刘、小艾 | 主力收入：教学关与日常单 |
| 高 | ¥800–1500 | ≈30% | 李老师(20000)、钱大叔(9000) | 大单目标：必须先挖需求（interest 门槛 40） |

\* 佣金率沿用现有梯度：**越便宜佣金率越高**（低绝对值×高比率 vs 高绝对值×低比率，鼓励服务穷人也奖励大单）。

## B3. 常规货备选池（逐档列候选与取舍理由）

### 低价位（¥50–150）

| 商品 | 价格/佣金 | 判定 | 理由 |
| --- | --- | --- | --- |
| 👟 老人防滑棉拖 | ¥89 / ¥30 | **留任** | 全场唯一 <¥100：陈奶奶、老周仅有的入场券，砍掉它等于这两个角色退出经济系统 |
| 🥤 316不锈钢保温杯 | ¥99 / ¥35 | **留任** | 需求最普适（钱大叔晨练、老周面试、大刘加班），与棉拖场景不重叠（喝水分 vs 防滑） |
| 🧣 羊毛护膝 | ¥139 / ¥45 | **新入** | 李老师的「钥匙商品」：窗台晾护膝+空膏药盒的线索直指膝盖疼；她只认数据，需要检测报告话术才能开锁——钥匙好拿，锁难开，正好成对 |
| 🧦 加绒手套 | ¥59 / ¥20 | 落选 | 佣金 ¥20，要 5 单才抵 1 单拖把，走量体验太碎 |
| 🥚 土鸡蛋礼盒 | ¥79 / ¥25 | 落选 | 菜市场货与「上门推销员」人设冲突，且食品保质期在多日循环里是逻辑洞 |

### 中价位（¥150–400）

| 商品 | 价格/佣金 | 判定 | 理由 |
| --- | --- | --- | --- |
| 🧹 旋风3000拖把 | ¥299 / ¥80 | **留任** | 教学关主力，钱大叔「划算」人格直接对口，demo 对局资产，不动 |
| 🥡 保鲜盒套装 | ¥129* / ¥40 | **留任** | 钱大叔「剩菜心事」的低配钥匙——和特殊货「第二冰箱」构成同一需求的低配/荒诞高配对，是目录里最好的结构之一（*严格说是低价上限，归中低桥段） |
| 😴 荞麦保健枕 | ¥169 / ¥50 | **留任** | 大刘（睡眠差）与张姐（腰疼）双钥匙，中价位入门 |
| 🍳 钛金不粘锅 | ¥399 / ¥100 | **移入备选**（让位） | 与拖把同吃「家务」场景、价格带重叠；「少油健康」卖点又与护膝（健康诉求）撞车。不是坏商品，是位置不够好——留作轮换 |
| 🪑 人体工学坐垫 | ¥249 / ¥70 | **备选第一顺位** | 大刘加班久坐直接对症，「程序员买坐垫」完全成立；若后续要扩常规货，第一个进 |
| 📚 高考冲刺密卷 | ¥199 / ¥60 | 落选 | 张姐女儿的钥匙，但教育焦虑题材做商品容易把喜剧压沉，与游戏荒诞轻快的调性冲突 |
| 🍵 智能茶壶 | ¥299 / ¥75 | 落选 | 与保温杯功能重叠——目录原则：**每件商品必须占一个别人占不了的需求位** |

### 高价位（¥800–1500，新增档）

| 商品 | 价格/佣金 | 判定 | 理由 |
| --- | --- | --- | --- |
| 📱 老人智能手机（大字体长续航） | ¥1299 / ¥400 | **新入** | 钱大叔的「情感大单」：儿子在深圳一年一回，视频看孙子的心事从线索到台词全埋好了——全目录情感钩子最深的一件，且他攒的钱第一次有了去处 |
| 🛏️ 乳胶床垫 | ¥1899 / ¥500 | **备选第一顺位** | 李老师「养老 + 关节疼」的进阶钥匙（护膝是入门钥匙，床垫是大单）；¥1899 卡在她预算内但会动大半积蓄，成交戏剧性强。首批先不进：一件高价货先把「高价位循环」验证跑通 |
| 💆 足浴盆 | ¥499 / ¥150 | 落选（暂） | 三位老人都沾边看似普适，实则「谁都对症=谁都不是钥匙」；且卡在中间价位，和护膝/床垫重夈权 |
| 💺 按摩椅 | ¥6999 / —— | 落选 | 价格击穿常规档上限，喜剧浓度不够撑起特殊货，两头不靠 |

## B4. 特殊任务货修正（荒诞线，6 件池微调）

1. **佣金与价格脱钩，改按难度定**：★1=¥100 / ★2=¥200 / ★3=¥400 / ★4=¥800 / ★5=¥1500，
   价格 25% 奖金保留。修复「防猫背叛保险 ★3 佣金 ¥60 还不如拖把」的倒挂。
2. **「家庭火箭发射台」移出随机任务池，改为彩蛋**：¥99999 全场无人买得起，
   留在池里就是「刷到即死任务」。改为：每日任务池从其余 5 件里抽；
   火箭台以「第 7 天公司邮件的荒诞新闻 / 货底」形式出场（看到即喜剧，不强求卖掉）。
   它的命中.notes：彩蛋化后任务池 5 件全部有真实买家（冰箱→钱大叔/李老师、地下室→李老师、
   保险→陈奶奶/大刘、沙发→张姐/小艾、鸽子服→人人），随机刷任何一件都是活任务。

## B5. 最终推荐清单（常规 6→7，每日特殊 1）

| # | 商品 | 档 | 价格 | 佣金 | 需求位（钥匙） |
| --- | --- | --- | --- | --- | --- |
| 1 | 👟 老人防滑棉拖 | 低 | ¥89 | ¥30 | 入场券（陈奶奶/老周） |
| 2 | 🥤 316保温杯 | 低 | ¥99 | ¥35 | 普适 |
| 3 | 🥡 保鲜盒套装 | 低中 | ¥129 | ¥40 | 钱大叔·剩菜（第二冰箱的低配对） |
| 4 | 🧣 羊毛护膝 ★新 | 低中 | ¥139 | ¥45 | 李老师·膝盖（需检测报告话术） |
| 5 | 😴 荞麦保健枕 | 中 | ¥169 | ¥50 | 大刘·睡眠 / 张姐·腰 |
| 6 | 🧹 旋风3000拖把 | 中 | ¥299 | ¥80 | 教学关·划算 |
| 7 | 📱 老人智能手机 ★新 | 高 | ¥1299 | ¥400 | 钱大叔·儿子在深圳（interest≥40） |

- 常规 +1 件的成本只是背包多一行；换来的是「高价位大单」这条目标线。
- 钛金不粘锅转入备选池（轮换位），坐垫/床垫列为扩充第一、二顺位。
- 每件新商品实现时需补：`claims`（销售口径，§17 注入用）、`keywords`（提及检测）、
  `realFunction`（Server 底）、`commission`，并跑一遍 detectMentioned 关键词冲突检查
  （如「手机」不得误伤「电话」类闲聊——用「手机|智能机」双词命中策略）。

## B6. 商品 × NPC 匹配矩阵（观察推理玩法的核心资产，客户端不可见）

| | 棉拖 | 保温杯 | 保鲜盒 | 护膝 | 荞麦枕 | 拖把 | 手机 | 第二冰箱 | 地下室 | 防猫险 | 防尴尬沙发 | 防鸽子服 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 钱大叔 9000 | ✓ | ✓ | 🔑 | ✓ | ✓ | 🔑 | 💰 | 🔑 | ✗ | ✗ | ✓ | ✓ |
| 李老师 20000 | ✓ | ✓ | ✓ | 🔑 | ✓ | ✓ | ✓ | ✓ | 💰 | ✗ | ✓ | ✓ |
| 张姐 4000 | ✓ | ✓ | ✓ | ✓ | 🔑 | ✓ | ✗ | ✓ | ✗ | ✓ | 🔑 | ✓ |
| 陈奶奶 1200 | 🔑 | ✓ | ✓ | ✓ | ✓ | ✗* | ✗ | ✗ | ✗ | 🔑 | ✗ | ✓ |
| 老周 300 | 🔑 | ✗* | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ |
| 大刘 8000 | ✗ | ✓ | ✗ | ✗ | 🔑 | ✗ | ✓ | ✗ | ✗ | 🔑 | ✓ | ✓ |
| 小艾 6000 | ✓ | ✓ | ✓ | ✗ | ✓ | ✓ | ✓ | ✓ | ✗ | ✓ | ✓ | 🔑 |

🔑 = 钥匙商品（有心事直接对症，配合条件系统有解锁台词）　💰 = 大单　✗* = 买得起但不合需求（死货是设计，逼玩家换目标）

## B7. 实现顺序建议

1. **A9 披露账本 + A4 conditionHints**（同一批改动）——knowledge/worries 结构化是两者共同的地基；
   一起改 shared 类型（LLMResponse 加 `revealed` / `conditionHints`）、npcData、promptBuilder、
   glmAdapter 校验链、ConversationService 记账，再补 dayloop-check 断言 9-12。
2. **A1-A8 条件闸门 + interest 门槛 + noInstall 关键词**——纯服务器逻辑 + 断言 1-8。
3. **B4 特殊货佣金修正**——3 行改动，立刻修复倒挂。
4. **B5 新商品**——补两件商品数据 + 矩阵核对，无新机制。
5. 护膝/手机的 claims 与 keywords 进 prompt 后，跑一轮 10 项 Vertical Slice 人工验收（PHASE 19）。
